# Backend App

Placeholder for the future Flask/API application.

Expected minimum shape when the app layer is added:

```text
app/
  db.py
  blueprints/
    ingest/
    declarations/
    health/
  services/
```

For now, keep this folder light. The active QAS execution remains in `Graph/` and `Integration_Layer/FLOW_V3/`.
