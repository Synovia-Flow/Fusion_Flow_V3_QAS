; =====================================================================
; Fusion Flow V3 QAS - database connection (TEMPLATE)
; Copy this file to  Fusion_Flow_QAS.ini  (same folder) and fill in the
; real password. The real .ini is GITIGNORED and must never be committed.
; =====================================================================

[database]
driver   = {ODBC Driver 17 for SQL Server}
server   = futureworks-sdi-db.database.windows.net
database = Fusion_Flow_V3_QAS
user     = SynFW_DB
password = <SET_IN_LOCAL_INI_OR_KEY_VAULT>
encrypt  = yes
trust_server_certificate = no
